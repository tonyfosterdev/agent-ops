apiVersion: 1

providers:
  - name: 'AgentOps Dashboards'
    orgId: 1
    folder: 'AgentOps'
    folderUid: 'agentops-folder'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
